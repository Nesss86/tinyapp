<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />

    <!-- Bootstrap CSS (included in _header.ejs) -->
    <title>TinyApp</title>
  </head>
  <body>
    <!-- Include Header Partial with userEmail passed in -->
    <%- include('partials/_header', { userEmail: userEmail }) %> 

    <main style="margin: 1em;">
      <h3>My URLs</h3>
      <table class="table">
        <thead>
          <tr>
            <th scope="col">Short URL ID</th>
            <th scope="col">Long URL</th>
            <th scope="col">Actions</th>
          </tr>
        </thead>
        <tbody>
          <% for (let id in urls) { %>
            <tr>
              <td><%= id %></td>
              <td><%= urls[id] %></td>
              <td>
                <a href="/urls/<%= id %>" class="btn btn-warning btn-sm">Edit</a>
                <form method="POST" action="/urls/<%= id %>/delete" style="display: inline;">
                  <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                </form>
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
      <!-- Add link to shorten a new URL -->
      <a href="/urls/new" class="btn btn-primary mt-3">Shorten a New URL</a>
    </main>
  </body>
</html>



